# Backend
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3
DB_URL=sqlite:///./app.db
UPLOAD_DIR=uploads
FAISS_INDEX_PATH=faiss_index

# Optional: use OpenAI instead of Ollama for LLM and embeddings
# OPENAI_API_KEY=sk-...

# Frontend (optional; defaults to /api when unset)
# VITE_API_URL=http://localhost:8000/api
